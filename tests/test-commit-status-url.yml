name: Test commit status URL and custom timezone
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        continue-on-error: true
        with:
          after: "18:00"
          before: "09:00"
          timezone: "Asia/Tokyo"
          prohibited-days-dates: "Saturday, Sunday"
          base-branches: "(default)"
          commit-status-context: "block-merge-based-on-time"
          commit-status-description-with-success: "Merge is allowed during business hours"
          commit-status-description-while-blocking: "Merge is blocked outside business hours"
          commit-status-url: "https://example.com/merge-policy"
          token: ${{ github.token }}

      - name: Assert action ran (succeeded or failed gracefully on sentinel repo)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ]; then
            echo "PASS: action succeeded"
          elif [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully (expected on sentinel repo or non-PR event)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
