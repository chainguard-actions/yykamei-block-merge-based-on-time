name: Block Merge Based on Time
on:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  block:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
      statuses: write
    steps:
      - uses: yykamei/block-merge-based-on-time@4563474068e843c13cc0510a82228171128d21ba # main
        id: block
        with:
          timezone: Asia/Tokyo
          after: 03:00
          before: 04:00
      - env:
          PR_BLOCKED: ${{ steps.block.outputs.pr-blocked }}
        run: echo "pr-blocked=$PR_BLOCKED"
        if: github.event_name == 'pull_request'
